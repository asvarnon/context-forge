# cargo-release configuration
# https://github.com/crate-ci/cargo-release/blob/master/docs/reference.md

# Only release from main
allow-branch = ["main"]

# All crates share one version (workspace.package.version)
shared-version = true
consolidate-commits = true

# We don't publish to crates.io
publish = false
# Skip cargo verification (we have CI for that)
verify = false

# Tag format: v0.2.0 (no crate prefix)
tag-name = "v{{version}}"
tag-message = "chore: release v{{version}}"

# Commit message
pre-release-commit-message = "chore: release v{{version}}"

# Push to origin
push-remote = "origin"

# Changelog generation is in crates/cli/release.toml (runs once, not per-crate)
# See: https://github.com/crate-ci/cargo-release/blob/master/docs/faq.md#how-do-i-apply-a-setting-only-to-one-crate-in-my-workspace
